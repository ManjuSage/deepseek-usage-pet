# 更新日志（CHANGELOG）

本项目遵循 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/)，版本号格式为 `MAJOR.MINOR.PATCH`。Git 标签使用 `v` 前缀（如 `v1.0.6`）。

> 说明：`1.0.6` 及之前的版本未严格遵循 SemVer（新增功能也使用了 PATCH 位）。项目已从 **`1.1.0`** 起按规范发布，不回改历史标签。

## [1.2.2] - 2026-09-12

**变更与修复**

- 日级报表按北京时间 2026-09-10 区分 Flash 两代：旧模型在切换日前保留历史名称，切换日及之后旧 Flash / Vision Exp 兼容别名统一按 V4.1 Flash 统计。
- 分时统计改为依据同步时观测到的 `deepseek-flash` 与旧 V4 ID 消失情况确定切换点，并持久化防止回退；V4.1 Flash 本身支持图片识别，不单独拆分视觉模型，未知模型 ID 原样保留；V4 Pro 始终保持独立。
- 修正文档中的模型能力说明：V4.1 Flash 自身支持图片识别，不再将独立的未来视觉模型作为统计口径假设。

## [1.2.1] - 2026-09-10

**变更与修复**

- 用量统计将 `deepseek-v4-flash` 与 `deepseek-flash` 合并显示为 **V4 Flash**，避免官方别名调整后同一模型被拆成两组。
- 保留 **V4 Flash Vision**、**V4.1 Flash** 与 **V4 Pro** 的独立显示和统计；V4.1 Flash 仅匹配当前已知的精确 ID 与到期日期变体，避免误吞未来视觉模型。
- 合并仅发生在报表查询层，数据库仍保留原始模型 ID，便于追溯平台账单。

## [1.2.0] - 2026-09-09

**新功能**

- 用量统计新增输入缓存命中率：顶部总览卡片展示总体命中率，每日走势支持按总体、模型或 API Key 查看命中率。
- 新增 DeepSeek V4.1 Flash 模型友好名称，并与 Flash 系列保持一致配色。

**变更与修复**

- 桌宠“今日已用”改为直接汇总平台实际费用，不再依赖本地价目表，自动适应 2026 年 9 月 10 日 Flash 调价及 V4 Pro 临时路由至 V4.1 Flash 的计费变化。
- Tokens 与费用的大数缩写改用中文“万 / 亿”单位。
- 限制平台登录 IPC 仅可由设置窗口和用量窗口调用。
- 修复 Windows 重复打包时旧的便携目录或 zip 导致产物整理失败。

**文档**

- 同步更新 README、DESIGN、AGENTS 与本更新日志中的功能、数据口径和计费策略说明。

## [1.1.0]

- 表情状态机（眨眼 / 生气 / 失望 / 害羞 / 疲惫）+ 表情总开关 + 分组折叠设置
- 表情台词 / 随机台词改为词条标签（chips）编辑器
- 气泡描边颜色改为取色器；表情高级参数可配（疲惫提示改为秒）
- 拖拽贴边吸附（靠近屏幕边缘才吸附）
- 「检查更新」功能（托盘 + 设置）
- 若干表情交互 / 气泡淡出 / 低余额提醒逻辑修复

## [1.0.6]

**修复**

- 修复「登录平台获取令牌」弹窗空白、无法登录的问题：权限请求处理器用错了 API（`webContents.setPermissionRequestHandler` 应为 `session.setPermissionRequestHandler`）。

## [1.0.5]

**新功能**

- 用量统计新增「总体」时间范围：查看**全部已记录数据**（最早到最晚，含中间空档）。
- 用量热力图新增**年份翻页**：标题栏右侧 `<` / `>` 可切换查看过去各年，默认当前年；热力图按完整周补齐，呈现规整长方形。
- README 新增「架构概览」图（Archify 生成的架构图）。

**修复**

- 修复热力图左右边缘标签被裁切（「1月 / 12月」「星期一二三…」只显示一半）：日历改为补齐完整周、方块恢复 16px，`<`/`>` 按钮移到标题栏，日历恢复完整宽度。
- `getDateRange()` 同时覆盖 `amount_daily` 与 `cost_daily`，避免只有费用、没有 token 的日期被漏掉。

## [1.0.4]

**新功能**

- 用量统计图表配色统一：模型 / 类型 / API Key 改为固定映射（Tableau 10 风格），每日走势、分时明细、饼图跨图颜色一致。
- 设置里的「立即刷新余额」现在也会弹出鲸鱼余额气泡（与托盘「立即刷新余额」行为一致）。
- 新增 Linux **AppImage** 自动构建（GitHub Actions），发布时随 Windows 产物一起提供。

**修复**

- 分时明细的模型名改为友好名（`V4 Pro` / `V4 Flash` 等），与每日走势、饼图一致，不再显示 `deepseek-v4-pro` 这类原始 ID。
- 图表颜色不再随数据出现顺序漂移；API Key 颜色按名称排序稳定分配。

## [1.0.3]

**新功能**

- 「镜像翻转」开关：设置里可开启/关闭；开启时随屏幕位置自动镜像，关闭时锁定当前方向，重启恢复默认。
- 运行日志系统：写入 `pet.log`（1MB 轮转），自动掩码 API Key / 平台令牌；设置「数据」页新增「打开日志」按钮。

**修复**

- 镜像改为「鲸鱼 + 气泡整体」翻转、旋转中心为两者合起来的中部，修复镜像后贴边漂移、被裁切的问题。
- 修复缩放后位移受限（被限制在某个长方形区域）与缩小后漂移：小屏动态限制最大缩放、多显示器锚点随屏更新、跨屏钳制统一、缩放改用一次原子 `setBounds`。
- 修复 Windows 终端中文日志乱码：终端回显按本机控制台代码页编码，日志文件保持 UTF-8。
- 移除 `main.js` 里 `WHALE_PET_TRACE` 守卫下的 `[trace] tick/delta` 调试日志。

## [1.0.2]

**安全与修复**

- 安全加固：`config:get` 密钥掩码、`config:set` 按 sender/key 限写、`shell:open-path` 白名单、登录窗守卫、原始响应存档上限。
- 取消同步后跳过后续步骤并尽早落盘；窗口 resize 增加上限；记账日期统一 GMT+8；用量 summary 改用 DOM 构造。

## [1.0.1]

- 参考父项目 [MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget) 同步更新。

## [1.0.0]

- 项目转为公开仓库；版本规范为 `1.0.0`。

## [0.1.0]

- 首个便携版与安装包。
